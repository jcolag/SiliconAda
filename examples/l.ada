   procedure Print_Roots is
   begin
        a := b + c * d / e - f;
        b := a + c + d + e + f;
        c := a - b - d - e - f;
        d := a * b * c * e * f;
        e := a / b / c / d / f;
        f := a - b / c * d + e;
   end Print_Roots;
